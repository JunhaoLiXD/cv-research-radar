# Optional. If absent, keyword-only analysis is used.
OPENAI_API_KEY=
# Required only when OPENAI_API_KEY is set. Choose a Structured Outputs-capable model.
OPENAI_MODEL=
# Optional. Semantic Scholar works with lower rate limits when unset.
SEMANTIC_SCHOLAR_API_KEY=
